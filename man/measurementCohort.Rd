% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/phenotypeTemplates.R
\name{measurementCohort}
\alias{measurementCohort}
\title{Measurement threshold cohort}
\usage{
measurementCohort(
  measurementConceptSet,
  valueFilter,
  unitConceptIds = NULL,
  washoutDays = 365L
)
}
\arguments{
\item{measurementConceptSet}{A `ConceptSet` for the measurement of interest.}

\item{valueFilter}{A value attribute (from `valueAsNumber()`, `rangeHigh()`,
or `rangeLow()`) wrapping a comparison operator such as `gt(5.7)` or
`lte(3.0)`.}

\item{unitConceptIds}{Optional integer vector of unit concept IDs to filter
the measurement unit (e.g. `8554L` for percent). Use `NULL` to accept any
unit.}

\item{washoutDays}{Minimum days of continuous observation required before
the index event. Default 365.}
}
\value{
A `Cohort` object.
}
\description{
Build a cohort based on a measurement value threshold: first qualifying
measurement per person with a minimum prior observation requirement, exiting
at end of continuous observation.
}
\examples{
\dontrun{
hba1cCs <- cs(descendants(3004410), name = "HbA1c")
uncontrolled <- measurementCohort(
  hba1cCs,
  valueFilter = valueAsNumber(gt(6.5)),
  unitConceptIds = 8554L
)
}
}
